fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic
python-dotenv
bcrypt
pyjwt
email-validator
python-multipart
# Phase 5 (POST /upload-analyze): pandas is used at runtime by
# app/upload_analysis.py to build the small feature frame passed to the
# existing ml/compliance rule engine, and python-multipart is required
# by FastAPI/Starlette for multipart file uploads (UploadFile).
pandas
python-multipart

# Phase 8 (GET /reports/generate, GET /reports/project/{id}): reportlab
# builds the PDF export at runtime (app/routes/reports.py). No other PDF
# dependency existed to preserve -- this is a new, additive capability.
reportlab

# --- Dev / testing only ---
pytest
httpx
scikit-learn