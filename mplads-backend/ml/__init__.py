"""
MPLADS AI Shield — ML engine package.

Phase 1 scope: preprocessing + Work ID normalization only
(ml/config.py, ml/preprocessing.py). Feature engineering, detectors,
risk fusion, and everything downstream are deliberately not present
yet — see ml/preprocessing.py's module docstring for the phase plan.
"""